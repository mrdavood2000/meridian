# Copy this file to .env (gitignored, never committed) and fill in real values.
# .env is loaded automatically wherever the meridian package is imported.
#
# Nothing is required yet - this file exists from day one so the habit (secrets
# live in .env, never in code or in git) is established before there's anything
# to protect. Real variables get added here the day the feed that needs them
# does.
